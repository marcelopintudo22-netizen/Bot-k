json
{
  "code": "bot.py\nimport re\nimport json\nimport sqlite3\nimport logging\nimport mercadopago\nimport qrcode\nfrom io import BytesIO \nimport asyncio\nfrom pathlib import Path\n\nfrom telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup\nfrom telegram.ext import (\n ApplicationBuilder, ContextTypes, CommandHandler, CallbackQueryHandler,\n    MessageHandler, filters, ConversationHandler\n)\n\n -------- CONFIG --------\nTOKEN = \"8278539911:AAEzoPTRB2ZkB0-rHmsYPL0yN98-3KiyYUU\"\nADMINIDS = [\"8786096698\"]\nPRODUCTSFILE = \"products.json\"  Caminho dos produtos em .json\nDBFILE = \"orders.db\"  Caminho do bando de dados local\nMERCADOPAGOTOKEN = \"TOKENDOMERCADOPAGOAQUI\"\nWEBHOOKURL = \"LINKDOSEUWEBHOOKAQUI\"  Indico Usar ngrok (Local)\nLIMITEESTOQUEBAIXO = 3  Quando o bot vai te avisar quando o estoque estiver baixo\nTAXACOMISSAOPARCEIRO = 0.50  Taxa de comissão do sistema de parceria\n ------------------------\n\nlogging.basicConfig(level=logging.INFO, format='%(asctime)s - %(name)s - %(levelname)s - %(message)s')\nlogger = logging.getLogger(name)\n\nif MERCADOPAGOTOKEN and \"SEUMERCADOPAGOACCESSTOKEN\" not in MERCADOPAGOTOKEN:\n    sdk = mercadopago.SDK(MERCADOPAGOTOKEN)\nelse:\n    sdk = None\n    logger.warning(\"Access Token do Mercado Pago não configurado.\")\n\nASKINGAMOUNT = range(1)\n\n --- Funções do Banco de Dados e Produtos ---\n\ndef initdb():\n    conn = sqlite3.connect(DBFILE, checksamethread=False)\n    c = conn.cursor()\n    c.execute(\"\"\"CREATE TABLE IF NOT EXISTS orders (id INTEGER PRIMARY KEY, userid INTEGER, username TEXT, productid TEXT, productname TEXT, price REAL, contact TEXT, status TEXT DEFAULT 'pending', createdat DATETIME DEFAULT CURRENTTIMESTAMP)\"\"\"\")\n    c.execute(\"\"\"CREATE TABLE IF NOT EXISTS accounts (id INTEGER PRIMARY KEY, productid TEXT, credentials TEXT, status TEXT DEFAULT 'available', orderid INTEGER, userid INTEGER, soldat DATETIME)\"\"\"\")\n    c.execute(\"\"\"CREATE TABLE IF NOT EXISTS users (userid INTEGER PRIMARY KEY, username TEXT, balance REAL DEFAULT 0.0, issupplier INTEGER DEFAULT 0, ispartner INTEGER DEFAULT 0, referredby INTEGER, joinedat DATETIME DEFAULT CURRENTTIMESTAMP)\"\"\"\")\n    c.execute(\"\"\"CREATE TABLE IF NOT EXISTS deposits (id INTEGER PRIMARY KEY, userid INTEGER, amount REAL, status TEXT DEFAULT 'pending', paymentid TEXT, qrcode TEXT, copiaecola TEXT, messageid INTEGER, createdat DATETIME DEFAULT CURRENTTIMESTAMP, paidat DATETIME, cancelled INTEGER DEFAULT 0)\"\"\"\")\n    c.execute(\"\"\"CREATE TABLE IF NOT EXISTS fakestock (productid TEXT PRIMARY KEY, quantity INTEGER NOT NULL DEFAULT 0)\"\"\"\")\n    c.execute(\"\"\"CREATE TABLE IF NOT EXISTS partnercommissions (id INTEGER PRIMARY KEY, partnerid INTEGER, referreduserid INTEGER, orderid INTEGER, amount REAL, createdat DATETIME DEFAULT CURRENTTIMESTAMP)\"\"\"\")\n    c.execute(\"\"\"CREATE TABLE IF NOT EXISTS promotion (id INTEGER PRIMARY KEY, isactive INTEGER DEFAULT 0, multiplierpercent REAL DEFAULT 0)\"\"\"\")\n    c.execute(\"INSERT OR IGNORE INTO promotion (id, isactive, multiplierpercent) VALUES (1, 0, 0)\")\n    \n    try: c.execute(\"ALTER TABLE deposits ADD COLUMN messageid INTEGER\")\n    except: pass\n    try: c.execute(\"ALTER TABLE users ADD COLUMN ispartner INTEGER DEFAULT 0\")\n    except: pass\n    try: c.execute(\"ALTER TABLE users ADD COLUMN referredby INTEGER\")\n    except: pass\n    try: c.execute(\"ALTER TABLE deposits ADD COLUMN cancelled INTEGER DEFAULT 0\")\n    except: pass\n    try: c.execute(\"ALTER TABLE users ADD COLUMN issupplier INTEGER DEFAULT 0\")\n    except: pass \n    conn.commit()\n    conn.close()\n\n     Função para padronizar diferentes formatos de credenciais para email:senha\ndef normalizecredentials(line):\n    line = line.strip()\n    \n     --- LISTA DE PADRÕES APRIMORADA ---\n     Cada padrão de Regex na lista tenta encontrar um formato de login.\n     Eles são testados em ordem.\n    patterns = [\n         Padrão 1 (Principal): Captura variações como \"User:\", \"Username:\", \"Login:\" e \"email:\"\n         seguido de \"Pass:\", \"Password:\" ou \"senha:\".\n         Cobre os exemplos:\n         Username: v.terezzino@uol.com.br Password: Ali@zz2vta\n         Login: cleiamcosta@gmail.com Password: 88357384820\n         User: cleiamcosta@gmail.com Pass:  88357384820\n        r'(?:user|username|login|email)[\\s:]([\\w\\.\\-]+@[\\w\\.\\-]+)[\\s,\\|]+(?:pass|password|senha)[\\s:](\\S+)',\n        \n         Padrão 2: Captura as abreviações \"usr:\" e \"pss:\".\n         Cobre os exemplos:\n         usr: cleiamcosta@gmail.com pss: cleiamcosta@gmail.com\n        r'(?:usr)[\\s:]([\\w\\.\\-]+@[\\w\\.\\-]+)[\\s,\\|]+(?:pss)[\\s:](\\S+)',\n        \n         Padrão 3: Captura o formato de bloco que inclui uma URL no início.\n        r'URL:.?Username:\\s([\\w\\.\\-]+@[\\w\\.\\-]+)\\sPassword:\\s(\\S+)',\n    ]\n\n     Tenta encontrar um padrão da nossa lista\n    for pattern in patterns:\n        match = re.search(pattern, line, re.IGNORECASE)  re.IGNORECASE faz com que não diferencie maiúsculas/minúsculas\n        if match:\n             Se encontrar, pega o email (grupo 1) e a senha (grupo 2)\n            email, password = match.groups()\n             Retorna no formato padronizado \"email:senha\"\n            return f\"{email.strip()}:{password.strip()}\"\n            \n     Se nenhum padrão complexo funcionar, tenta o formato simples (email:senha)\n    if '@' in line and ':' in line:\n        parts = line.split(':', 1)\n        if len(parts) == 2 and '@' in parts[0]:\n             Garante que não é uma linha de configuração como \"Host: ...\"\n            if not parts[0].lower().strip() in [\"soft\", \"url\", \"host\"]:\n                return f\"{parts[0].strip()}:{parts[1].strip()}\"\n            \n     Se absolutamente nada for reconhecido, retorna a linha como estava para análise manual\n    return line\n\ndef loadproducts():\n    try:\n        with open(PRODUCTSFILE, \"r\", encoding=\"utf-8\") as f: return json.load(f)\n    except FileNotFoundError: return []\nPRODUCTS = loadproducts()\n\ndef initfakestock():\n    conn = sqlite3.connect(DBFILE)\n    c = conn.cursor()\n    for product in PRODUCTS:\n        c.execute(\"INSERT OR IGNORE INTO fakestock (productid, quantity) VALUES (?, ?)\", (product['id'], 0))\n    conn.commit()\n    conn.close()\n    logger.info(\"Estoque falso (buffer) inicializado para todos os produtos.\")\n\ndef getorcreateuser(userid, username):\n    conn = sqlite3.connect(DBFILE, checksamethread=False); conn.rowfactory = sqlite3.Row; c = conn.cursor()\n    c.execute(\"SELECT  FROM users WHERE userid = ?\", (userid,))\n    user = c.fetchone()\n    isnewuser = user is None\n    if isnewuser:\n        c.execute(\"INSERT INTO users (userid, username) VALUES (?, ?)\", (userid, username)); conn.commit()\n        c.execute(\"SELECT  FROM users WHERE userid = ?\", (userid,)); user = c.fetchone()\n    conn.close()\n    return user, isnewuser\n\ndef productbyid(pid):\n    for p in PRODUCTS:\n        if p[\"id\"] == pid: return p\n    return None\n\nasync def start(update: Update, context: ContextTypes.DEFAULTTYPE):\n    user = update.effectiveuser\n    userdata, isnewuser = getorcreateuser(user.id, user.username or user.firstname)\n\n     Lógica para processar o link de indicação de parceiro\n    if isnewuser and context.args:\n        try:\n            partnerid = int(context.args[0])\n            if partnerid != user.id:\n                conn = sqlite3.connect(DBFILE)\n                c = conn.cursor()\n                c.execute(\"SELECT userid FROM users WHERE userid = ? AND ispartner = 1\", (partnerid,))\n                partnerexists = c.fetchone()\n                if partnerexists:\n                    c.execute(\"UPDATE users SET referredby = ? WHERE userid = ?\", (partnerid, user.id))\n                    conn.commit()\n                    logger.info(f\"Novo usuário {user.id} foi indicado pelo parceiro {partnerid}\")\n                    try:\n                        await context.bot.sendmessage(chatid=partnerid, text=f\"🎉 Um novo usuário (@{user.username or 'desconhecido'}) entrou no bot pelo seu link de parceiro!\")\n                    except Exception as e:\n                        logger.warning(f\"Não foi possível notificar o parceiro {partnerid}: {e}\")\n                conn.close()\n        except (ValueError, IndexError):\n            pass  Ignora se o código de indicação for inválido\n\n    text, replymarkup = buildmainmenu(userdata)\n    await update.message.replytext(text, replymarkup=replymarkup, parsemode=\"Markdown\")\n\n\nasync def checklowstock(productid, context: ContextTypes.DEFAULTTYPE):\n    product = productbyid(productid)\n    if not product: return\n    conn = sqlite3.connect(DBFILE)\n    c = conn.cursor()\n    c.execute(\"SELECT COUNT() FROM accounts WHERE productid = ? AND status = 'available'\", (productid,))\n    stockcount = c.fetchone()[0]\n    conn.close()\n    if stockcount > 0 and stockcount <= LIMITEESTOQUEBAIXO:\n        message = f\"⚠️ AVISO DE ESTOQUE BAIXO ⚠️\\n\\nO produto {product['name']} ({productid}) está com apenas {stockcount} unidade(s) em estoque.\"\n        for adminid in ADMINIDS:\n            try:\n                await context.bot.sendmessage(chatid=adminid, text=message, parsemode=\"Markdown\")\n            except Exception as e:\n                logger.error(f\"Falha ao enviar aviso de estoque baixo para o admin {adminid}: {e}\")\n\n --- Funções de Geração de Menus ---\ndef buildmainmenu(userdata):\n     NOVO: Verifica se a promoção está ativa para exibir um aviso\n    conn = sqlite3.connect(DBFILE); c = conn.cursor()\n    c.execute(\"SELECT isactive, multiplierpercent FROM promotion WHERE id = 1\")\n    promodata = c.fetchone()\n    conn.close()\n    \n    promobanner = \"\"\n    if promodata and promodata[0] == 1:\n        bonus = int(promodata[1])\n        promobanner = f\"🎉 PROMOÇÃO ATIVA! 🎉\\nDeposite e ganhe {bonus}% de bônus em saldo!\\n\\n\"\n\n    text = (\n        f\"{promobanner}\"  Banner da promoção aparece aqui\n        \"🛒 | Bem vindo(a) (Nome do seu bot aqui) \\n🌐 By - (Nome do seu bot aqui)\\n\\n\"\n        \"✅ (Informações de apresentação do seu bot aqui). \\n\"\n        \"✅ (Informações de apresentação do seu bot aqui) \\n\"\n        \"✅ (Informações de apresentação do seu bot aqui) \\n\\n\"\n        \"🔑 Termos Importantes:\\n\"\n        \"┣ 🚫 Use sempre 4G.\\n\"\n        \"┣ ⌛ Trocas: 10 minutos, com print.\\n\"\n        \"┗ ⚠️ Não damos garantia de aprovação do seu pedido.\\n\\n\"\n        \"👤 Seu perfil:\\n\"\n        f\"┣ 🆔 ID: {userdata['userid']}\\n\"\n        f\"┗ 💰 Saldo: R$ {userdata['balance']:.2f}\"\n    )\n    keyboard = [\n        [InlineKeyboardButton(\"🔑 Produtos aqui\", callbackdata=\"menucategories\"), InlineKeyboardButton(\"💰 Adicionar Saldo\", callbackdata=\"depositstart\")],\n        [InlineKeyboardButton(\"🛒 Minhas Compras\", callbackdata=\"menumyaccounts\")],\n        [InlineKeyboardButton(\"🛍️ Para Revendedores\", callbackdata=\"menusupplierscategories\")],\n        [InlineKeyboardButton(\"📜 Termos\", callbackdata=\"showterms\")],\n        [InlineKeyboardButton(\"💬 Grupo\", url=\"LINKDOSEUGRUPOAQUI\"), InlineKeyboardButton(\"💡 Suporte\", url=\"LINKDOSEUSUPORTEAQUI\")]\n    ]\n    return text, InlineKeyboardMarkup(keyboard)\n\ndef buildcategoriesmenu(forsupplier=False):\n    categories = sorted(list(set(p.get('category', 'Outros') for p in PRODUCTS)))\n    prefix = \"suppliercategory\" if forsupplier else \"category\"\n    keyboard = [[InlineKeyboardButton(cat, callbackdata=f\"{prefix}|{cat}\")] for cat in categories]\n    keyboard.append([InlineKeyboardButton(\"⬅️ Voltar\", callbackdata=\"backtomain\")])\n    title = \"🗂️ | (Informações de apresentação do seu bot aqui)\\n🌐 (Nome do seu bot aqui)\" if forsupplier else \"🗂️ | (Informações de apresentação do seu bot aqui)\\n🌐 By - (Nome do seu bot aqui)\"\n    text = f\"{title}\\n\\n(Informações de apresentação do seu bot aqui):\"\n    return text, InlineKeyboardMarkup(keyboard)\n\ndef buildproductsmenu(userdata, category, forsupplier=False):\n    conn = sqlite3.connect(DBFILE)\n    c = conn.cursor()\n    productidsincategory = tuple([p['id'] for p in PRODUCTS if p.get('category') == category])\n    totalvisualstock = 0\n    if productidsincategory:\n        placeholders = ', '.join('?' for  in productidsincategory)\n        queryreal = f\"SELECT COUNT() FROM accounts WHERE status = 'available' AND productid IN ({placeholders})\"\n        realstock = c.execute(queryreal, productidsincategory).fetchone()[0]\n        queryfake = f\"SELECT SUM(quantity) FROM fakestock WHERE productid IN ({placeholders})\"\n        resultfake = c.execute(queryfake, productidsincategory).fetchone()\n        fakestock = resultfake[0] if resultfake and resultfake[0] is not None else 0\n        totalvisualstock = realstock + fakestock\n    conn.close()\n    headertext = (\n        f\"🛒 (Nome do seu bot aqui) | (Informações de apresentação do seu bot aqui)\\n\\n\"\n        f\"🔎 Total de produtos disponíveis: {totalvisualstock}\\n\\n\"\n        f\"👤 Seu perfil:\\n\"\n        f\"┣ 🆔 ID: {userdata['userid']}\\n\"\n        f\"┗ 💰 Saldo: R$ {userdata['balance']:.2f}\\n\\n\"\n        f\"Alguma Dúvida ou Problema?\\n🙋‍♂️ Entre em contato com suporte @ do seu suporte\\n\\n\"\n        f\"Escolha um produto para comprar\"\n    )\n    keyboard = []\n    row = [] \n    prefix = \"supplierbuy\" if forsupplier else \"buy\"\n    backcallback = \"menusupplierscategories\" if forsupplier else \"menucategories\"\n    productsincategory = [p for p in PRODUCTS if p.get('category', 'Outros') == category]\n    for p in productsincategory:\n        price = p.get('supplierprice', p['price']) if forsupplier else p['price']\n        if forsupplier and 'supplierprice' not in p:\n            continue\n        buttontext = f\"🔑 {p['name']} | R$ {price:.2f}\"\n        button = InlineKeyboardButton(buttontext, callbackdata=f\"{prefix}|{p['id']}\")\n        row.append(button)\n        if len(row) == 2:\n            keyboard.append(row)\n            row = []\n    if row:\n        keyboard.append(row)\n    keyboard.append([InlineKeyboardButton(\"⬅️ Voltar\", callbackdata=backcallback)])\n    return headertext, InlineKeyboardMarkup(keyboard)\n\ndef buildmyaccountsmenu(userid):\n    conn = sqlite3.connect(DBFILE); conn.rowfactory = sqlite3.Row; c = conn.cursor()\n    c.execute(\"SELECT id, productname, createdat FROM orders WHERE userid = ? AND status = 'delivered' ORDER BY createdat DESC LIMIT 25\", (userid,))\n    orders = c.fetchall(); conn.close()\n    if not orders:\n        text = \"📦 Minhas Compras\\n\\nVocê ainda não comprou nenhuma conta.\"\n        keyboard = [[InlineKeyboardButton(\"⬅️ Voltar\", callbackdata=\"backtomain\")]]\n    else:\n        text = \"📦 Minhas Compras\\n\\nClique em um pedido para ver os dados da conta.\"\n        keyboard = [[InlineKeyboardButton(f\"{o['productname']} ({o['createdat'].split(' ')[0]})\", callbackdata=f\"showaccount|{o['id']}\")] for o in orders]\n        keyboard.append([InlineKeyboardButton(\"⬅️ Voltar\", callbackdata=\"backtomain\")])\n    return text, InlineKeyboardMarkup(keyboard)\n\n NOVO COMANDO: Cole em bot.py (junto com os outros comandos de admin)\nasync def listpartners(update: Update, context: ContextTypes.DEFAULTTYPE):\n    if update.message.fromuser.id not in ADMINIDS: return\n\n    conn = sqlite3.connect(DBFILE)\n    conn.rowfactory = sqlite3.Row\n    c = conn.cursor()\n\n     Query otimizada para buscar parceiros, contagem de indicados e soma de comissões\n    c.execute(\"\"\"\n        SELECT \n            u.userid, \n            u.username, \n            (SELECT COUNT() FROM users WHERE referredby = u.userid) as referralcount,\n            (SELECT SUM(amount) FROM partnercommissions WHERE partnerid = u.userid) as totalcommission\n        FROM users u\n        WHERE u.ispartner = 1\n    \"\"\")\n    \n    partners = c.fetchall()\n    conn.close()\n\n    if not partners:\n        await update.message.replytext(\"Nenhum parceiro encontrado.\")\n        return\n\n    text = \"👥 <b>Lista de Parceiros</b> 👥\\n\\n\"\n    totalpartners = len(partners)\n    totalcommissionspaid = 0\n\n    for partner in partners:\n        userid = partner['userid']\n        username = f\"(@{partner['username']})\" if partner['username'] else \"(Sem @username)\"\n        referrals = partner['referralcount'] or 0\n        commission = partner['totalcommission'] or 0.0\n        totalcommissionspaid += commission\n        \n        text += (\n            f\"▪️ <b>Parceiro:</b> <code>{userid}</code> {username}\\n\"\n            f\"   - <b>Indicados:</b> {referrals}\\n\"\n            f\"   - <b>Comissão Total:</b> <code>R$ {commission:.2f}</code>\\n\\n\"\n        )\n\n    text += f\"----\\n<b>Total de Parceiros:</b> {totalpartners}\\n\"\n    text += f\"<b>Total de Comissões:</b> <code>R$ {totalcommissionspaid:.2f}</code>\"\n\n    await update.message.replytext(text, parsemode=\"HTML\")\n\nasync def partnerstats(update: Update, context: ContextTypes.DEFAULTTYPE):\n    if update.message.fromuser.id not in ADMINIDS: return\n    if len(context.args) != 1:\n        await update.message.replytext(\"Uso: /partnerstats <partnerid>\")\n        return\n    try:\n        partnerid = int(context.args[0])\n        conn = sqlite3.connect(DBFILE); conn.rowfactory = sqlite3.Row; c = conn.cursor()\n        partnerdata = c.execute(\"SELECT  FROM users WHERE userid = ? AND ispartner = 1\", (partnerid,)).fetchone()\n        if not partnerdata:\n            await update.message.replytext(\"Este usuário não é um parceiro ou não foi encontrado.\"); conn.close(); return\n        \n        c.execute(\"SELECT COUNT() as count FROM users WHERE referredby = ?\", (partnerid,))\n        referralcount = c.fetchone()['count']\n        c.execute(\"SELECT SUM(amount) as total FROM partnercommissions WHERE partnerid = ?\", (partnerid,))\n        totalcommissionraw = c.fetchone()['total']\n        totalcommission = totalcommissionraw or 0.0\n        conn.close()\n        \n        text = (\n            f\"📊 Estatísticas do Parceiro - @{partnerdata['username']} ({partnerid})\\n\\n\"\n            f\"👥 Total de Indicados: {referralcount}\\n\"\n            f\"💰 Total de Comissões Geradas: R$ {totalcommission:.2f}\"\n        )\n        await update.message.replytext(text, parsemode=\"Markdown\")\n    except (ValueError, IndexError):\n        await update.message.replytext(\"ID de parceiro inválido.\")\n\nasync def partnershipcommand(update: Update, context: ContextTypes.DEFAULTTYPE):\n    user = update.effectiveuser\n    userdata,  = getorcreateuser(user.id, user.username or user.firstname)\n\n    if not userdata['ispartner']:\n        await update.message.replytext(\"Você não faz parte do nosso programa de parceiros. Entre em contato com o suporte para saber mais.\")\n        return\n        \n    conn = sqlite3.connect(DBFILE, checksamethread=False); conn.rowfactory = sqlite3.Row; c = conn.cursor()\n    \n     Busca o número de indicados\n    c.execute(\"SELECT COUNT() as count FROM users WHERE referredby = ?\", (user.id,))\n    referralcount = c.fetchone()['count']\n    \n     --- CORREÇÃO APLICADA AQUI ---\n     Soma o total de comissões de forma mais segura\n    c.execute(\"SELECT SUM(amount) as total FROM partnercommissions WHERE partnerid = ?\", (user.id,))\n    result = c.fetchone()\n    totalcommission = result['total'] if result and result['total'] is not None else 0.0\n     --- FIM DA CORREÇÃO ---\n    \n    conn.close()\n    \n    referrallink = f\"https://t.me/{context.bot.username}?start={user.id}\"\n    \n    text = (\n        \"📈 Seu Painel de Parceiro\\n\\n\"\n        f\"👥 Total de Indicados: {referralcount}\\n\"\n        f\"💰 Total de Comissões Geradas: R$ {totalcommission:.2f}\\n\\n\"\n        \"Use o link abaixo para convidar novos usuários. Para cada compra que eles fizerem, você ganhará uma comissão!\\n\\n\"\n        \"Seu Link de Parceiro:\\n\"\n        f\"{referrallink}\"\n    )\n    await update.message.replytext(text, parsemode=\"Markdown\")\n\n     NOVO: Comandos de Admin para gerenciar a promoção\nasync def promoon(update: Update, context: ContextTypes.DEFAULTTYPE):\n    if update.message.fromuser.id not in ADMINIDS: return\n    if not context.args or len(context.args) != 1:\n        await update.message.replytext(\"Uso: /promoon <porcentagem>\\nExemplo: /promoon 25 para 25% de bônus.\", parsemode=\"Markdown\")\n        return\n    try:\n        percent = float(context.args[0])\n        if percent <= 0:\n            await update.message.replytext(\"A porcentagem deve ser um número positivo.\")\n            return\n    except ValueError:\n        await update.message.replytext(\"Por favor, envie uma porcentagem válida.\")\n        return\n    \n    conn = sqlite3.connect(DBFILE); c = conn.cursor()\n    c.execute(\"UPDATE promotion SET isactive = 1, multiplierpercent = ? WHERE id = 1\", (percent,))\n    conn.commit(); conn.close()\n    \n    await update.message.replytext(f\"✅ Promoção ATIVADA com {percent}% de bônus! Todos os novos depósitos serão bonificados.\", parsemode=\"Markdown\")\n\nasync def promooff(update: Update, context: ContextTypes.DEFAULTTYPE):\n    if update.message.fromuser.id not in ADMINIDS: return\n    conn = sqlite3.connect(DBFILE); c = conn.cursor()\n    c.execute(\"UPDATE promotion SET isactive = 0, multiplierpercent = 0 WHERE id = 1\")\n    conn.commit(); conn.close()\n    await update.message.replytext(\"❌ Promoção DESATIVADA. Os depósitos voltam ao normal.\", parsemode=\"Markdown\")\n\nasync def promostatus(update: Update, context: ContextTypes.DEFAULTTYPE):\n    if update.message.fromuser.id not in ADMINIDS: return\n    conn = sqlite3.connect(DBFILE); c = conn.cursor()\n    c.execute(\"SELECT isactive, multiplierpercent FROM promotion WHERE id = 1\")\n    isactive, percent = c.fetchone()\n    conn.close()\n\n    if isactive:\n        await update.message.replytext(f\"ℹ️ A promoção está ATIVA com um bônus de {percent}%.\", parsemode=\"Markdown\")\n    else:\n        await update.message.replytext(\"ℹ️ Nenhuma promoção está ativa no momento.\", parsemode=\"Markdown\")\n\n Comando de Admin para adicionar parceiro\nasync def addpartner(update: Update, context: ContextTypes.DEFAULTTYPE):\n    if update.message.fromuser.id not in ADMINIDS: return\n    if len(context.args) != 1:\n        await update.message.replytext(\"Uso: /addpartner <userid>\")\n        return\n    try:\n        useridtoadd = int(context.args[0])\n        conn = sqlite3.connect(DBFILE)\n        c = conn.cursor()\n        c.execute(\"UPDATE users SET ispartner = 1 WHERE userid = ?\", (useridtoadd,))\n        if c.rowcount == 0:\n            await update.message.replytext(f\"Usuário {useridtoadd} não encontrado. Ele precisa iniciar o bot primeiro.\")\n        else:\n            conn.commit()\n            await update.message.replytext(f\"✅ Usuário {useridtoadd} agora é um Parceiro.\")\n            await context.bot.sendmessage(chatid=useridtoadd, text=\"🎉 Parabéns! Você agora faz parte do nosso programa de parceiros. Use o comando /parceria para ver seu painel.\")\n        conn.close()\n    except (ValueError, IndexError):\n        await update.message.replytext(\"ID de usuário inválido.\")\n\n Comando de Admin para remover parceiro\nasync def removepartner(update: Update, context: ContextTypes.DEFAULTTYPE):\n    if update.message.fromuser.id not in ADMINIDS: return\n    if len(context.args) != 1:\n        await update.message.replytext(\"Uso: /rempartner <userid>\")\n        return\n    try:\n        useridtoremove = int(context.args[0])\n        conn = sqlite3.connect(DBFILE)\n        c = conn.cursor()\n        c.execute(\"UPDATE users SET ispartner = 0 WHERE userid = ?\", (useridtoremove,))\n        if c.rowcount == 0:\n            await update.message.replytext(f\"Usuário {useridtoremove} não encontrado.\")\n        else:\n            conn.commit()\n            await update.message.replytext(f\"✅ Usuário {useridtoremove} não é mais um Parceiro.\")\n        conn.close()\n    except (ValueError, IndexError):\n        await update.message.replytext(\"ID de usuário inválido.\")\n\nasync def pixcommand(update: Update, context: ContextTypes.DEFAULTTYPE):\n    user = update.effectiveuser\n    if not context.args:\n        await update.message.replytext(\"Uso: /pix <valor>\\nExemplo: /pix 10.50\", parsemode=\"Markdown\")\n        return\n    try:\n        amount = float(context.args[0].replace(\",\", \".\"))\n        if amount <= 1:\n            await update.message.replytext(\"❌ O valor do depósito deve ser maior que R$ 1,00.\")\n            return\n    except (ValueError, IndexError):\n        await update.message.replytext(\"❌ Valor inválido. Por favor, envie um número. Exemplo: /pix 10.50\", parsemode=\"Markdown\")\n        return\n    if not sdk:\n        await update.message.replytext(\"Sistema de pagamento indisponível no momento.\")\n        return\n\n    paymentdata = {\n        \"transactionamount\": amount,\n        \"description\": f\"Crédito para {user.id}\",\n        \"paymentmethodid\": \"pix\",\n        \"payer\": {\"email\": f\"usuario{user.id}@teubot.com\"},\n        \"notificationurl\": f\"{WEBHOOKURL}/webhook/mercado-pago\"\n    }\n\n    try:\n        payment = sdk.payment().create(paymentdata)[\"response\"]\n        copiaecola = payment[\"pointofinteraction\"][\"transactiondata\"][\"qrcode\"]\n        \n         Gera a imagem do QR Code em memória\n        qrimg = qrcode.make(copiaecola)\n        buf = BytesIO()\n        qrimg.save(buf, 'PNG')\n        buf.seek(0)\n\n        conn = sqlite3.connect(DBFILE)\n        c = conn.cursor()\n        c.execute(\"INSERT INTO deposits (userid, amount, paymentid, copiaecola) VALUES (?, ?, ?, ?)\", (user.id, amount, payment[\"id\"], copiaecola))\n        conn.commit()\n        depositid = c.lastrowid\n        \n        caption = (\n            f\"✅ PIX Gerado para R$ {amount:.2f}!\\n\\n\"\n            \"Escaneie o QR Code ou use o código abaixo:\\n\\n\"\n            f\"{copiaecola}\\n\\n\"\n            \"O saldo será creditado automaticamente após o pagamento.\"\n        )\n        keyboard = [[InlineKeyboardButton(\"❌ Cancelar este Depósito\", callbackdata=f\"canceldeposit|{depositid}\")]]\n        \n         --- ATUALIZAÇÃO APLICADA AQUI ---\n         1. Envia a foto e ARMAZENA a mensagem enviada\n        sentmessage = await context.bot.sendphoto(\n            chatid=user.id,\n            photo=buf,\n            caption=caption,\n            replymarkup=InlineKeyboardMarkup(keyboard),\n            parsemode=\"Markdown\"\n        )\n        \n         2. Salva o ID da mensagem no banco de dados para uso futuro\n        c.execute(\"UPDATE deposits SET messageid = ? WHERE id = ?\", (sentmessage.messageid, depositid))\n        conn.commit()\n        conn.close()\n         --- FIM DA ATUALIZAÇÃO ---\n\n    except Exception as e:\n        logger.error(f\"Erro ao gerar PIX via comando para {user.id}: {e}\")\n        await update.message.replytext(\"❌ Ocorreu um erro ao gerar a cobrança PIX. Tente novamente mais tarde.\")\n\n --- Roteador de Menus ---\nasync def mainmenurouter(update: Update, context: ContextTypes.DEFAULTTYPE):\n    query = update.callbackquery; await query.answer()\n    user = query.fromuser\n    userdata,  = getorcreateuser(user.id, user.username or user.firstname)\n    data = query.data\n    text, replymarkup = None, None\n\n    if data == 'backtomain': \n        text, replymarkup = buildmainmenu(userdata)\n    \n     --- LÓGICA DE CANCELAMENTO CORRIGIDA ---\n    elif data.startswith('canceldeposit|'):\n        depositid = int(data.split('|')[1])\n        \n        conn = sqlite3.connect(DBFILE)\n        conn.rowfactory = sqlite3.Row\n        c = conn.cursor()\n        \n        c.execute(\"SELECT paymentid FROM deposits WHERE id = ? AND userid = ? AND status = 'pending'\", (depositid, user.id))\n        deposit = c.fetchone()\n        \n        if not deposit:\n            await query.answer(\"⚠️ Este PIX não pode mais ser cancelado.\", showalert=True)\n            conn.close()\n            return\n            \n        paymentid = deposit['paymentid']\n\n        try:\n            cancellationdata = {\"status\": \"cancelled\"}\n            result = sdk.payment().update(paymentid, cancellationdata)\n\n            if result and result[\"status\"] == 200:\n                c.execute(\"UPDATE deposits SET cancelled = 1, status = 'cancelled' WHERE id = ?\", (depositid,))\n                conn.commit()\n                \n                 --- CORREÇÃO APLICADA AQUI ---\n                 1. Edita a legenda para mostrar a confirmação\n                await query.editmessagecaption(\n                    caption=\"✅ Depósito PIX cancelado com sucesso.\",\n                    replymarkup=None \n                )\n                \n                 2. Espera 3 segundos\n                await asyncio.sleep(3)\n                \n                 3. Apaga a mensagem do QR Code\n                await query.message.delete()\n                \n                 4. Envia o menu inicial como uma nova mensagem\n                text, replymarkup = buildmainmenu(userdata)\n                await context.bot.sendmessage(\n                    chatid=user.id,\n                    text=text,\n                    replymarkup=replymarkup,\n                    parsemode=\"Markdown\"\n                )\n                 --- FIM DA CORREÇÃO ---\n                \n                logger.info(f\"Pagamento {paymentid} cancelado com sucesso pelo usuário {user.id}.\")\n            else:\n                await query.answer(\"⚠️ Não foi possível cancelar o PIX no Mercado Pago. Ele pode ter sido pago ou expirado.\", showalert=True)\n                logger.warning(f\"Falha ao tentar cancelar o pagamento {paymentid} na API do MP. Resposta: {result}\")\n\n        except Exception as e:\n            await query.answer(\"❌ Ocorreu um erro de comunicação ao tentar cancelar o PIX.\", showalert=True)\n            logger.error(f\"Erro na API do MP ao cancelar {paymentid}: {e}\")\n        finally:\n            conn.close()\n        \n        return \n\n    elif data == 'showterms':\n        text = (\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n            \"Seus termos aqui\"\n        )\n        keyboard = [[InlineKeyboardButton(\"⬅️ Voltar ao Menu\", callbackdata=\"backtomain\")]]\n        replymarkup = InlineKeyboardMarkup(keyboard)\n        \n    elif data == 'menucategories': \n        text, replymarkup = buildcategoriesmenu(forsupplier=False)\n        \n    elif data == 'menusupplierscategories':\n        if userdata['issupplier'] == 1:\n            text, replymarkup = buildcategoriesmenu(forsupplier=True)\n        else:\n            await query.answer(\"🚫 Você não tem permissão para acessar esta área.\", showalert=True)\n            return\n            \n    elif data == 'menumyaccounts': \n        text, replymarkup = buildmyaccountsmenu(user.id)\n        \n    elif data.startswith('category|'): \n        text, replymarkup = buildproductsmenu(userdata, data.split('|', 1)[1], forsupplier=False)\n        \n    elif data.startswith('suppliercategory|'): \n        text, replymarkup = buildproductsmenu(userdata, data.split('|', 1)[1], forsupplier=True)\n        \n    elif data.startswith('showaccount|'):\n        orderid = int(data.split('|', 1)[1])\n        conn = sqlite3.connect(DBFILE); conn.rowfactory = sqlite3.Row; c = conn.cursor()\n        account = c.execute(\"SELECT credentials FROM accounts WHERE orderid = ? AND userid = ?\", (orderid, user.id)).fetchone(); conn.close()\n        if account:\n            text = f\"🔑 Dados da sua Conta\\n\\n\\n{account['credentials']}\\n\"\n            keyboard = [[InlineKeyboardButton(\"⬅️ Voltar\", callbackdata=\"menumyaccounts\")]]\n            replymarkup = InlineKeyboardMarkup(keyboard)\n        else: \n            text, replymarkup = buildmyaccountsmenu(user.id)\n            \n    if text and replymarkup: \n        await query.editmessagetext(text, replymarkup=replymarkup, parsemode=\"Markdown\")\n        \n --- Lógica de Compra e Depósito ---\nasync def buycallbackhandler(update: Update, context: ContextTypes.DEFAULTTYPE):\n    query = update.callbackquery; await query.answer()\n    user = query.fromuser\n    userdata,  = getorcreateuser(user.id, user.username or user.firstname)\n    issupplierbuy = query.data.startswith(\"supplierbuy|\")\n    productid = query.data.split(\"|\")[1]\n    product = productbyid(productid)\n    \n    if not product:\n        await query.message.replytext(\"❌ Produto não encontrado.\")\n        return\n    if issupplierbuy and userdata['issupplier'] != 1:\n        await query.answer(\"🚫 Você não tem permissão para comprar como fornecedor.\", showalert=True)\n        return\n        \n    pricetouse = product['supplierprice'] if issupplierbuy and 'supplierprice' in product else product['price']\n    \n    if userdata['balance'] < pricetouse:\n        await query.message.replytext(f\"Saldo insuficiente.\")\n        return\n    \n    conn = sqlite3.connect(DBFILE, checksamethread=False); conn.rowfactory = sqlite3.Row; c = conn.cursor()\n    c.execute(\"SELECT  FROM accounts WHERE productid = ? AND status = 'available' LIMIT 1\", (productid,))\n    account = c.fetchone()\n    \n    if not account:\n        await query.message.replytext(f\"{product['name']} fora de estoque.\", parsemode=\"Markdown\")\n        conn.close()\n        return\n    \n    try:\n        c.execute(\"UPDATE users SET balance = balance - ? WHERE userid = ?\", (pricetouse, user.id))\n        c.execute(\"INSERT INTO orders (userid, username, productid, productname, price, status) VALUES (?, ?, ?, ?, ?, ?)\", (user.id, user.username, productid, product['name'], pricetouse, 'delivered'))\n        orderid = c.lastrowid\n        c.execute(\"UPDATE accounts SET status = 'sold', orderid = ?, userid = ?, soldat = CURRENTTIMESTAMP WHERE id = ?\", (orderid, user.id, account['id']))\n        \n         --- LÓGICA DE COMISSÃO CORRIGIDA E MAIS ROBUSTA ---\n         Busca os dados do usuário novamente
